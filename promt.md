# План реализации: Баланс смены + Таймлайн + Виды простоев

## Суть задачи
Часто бывает недовыполнение плана из-за неучтённых простоев.  
Пример: за 3 часа должно быть 130 шт, а сделано 100 → значит ~15 мин простоя не записаны.  
Цель: мастер сразу видит расхождение и понимает, что забыл указать простой.

---

## 1. Изменить виды простоев

Заменить текущие пресеты на:

| Пресет | Длительность |
|--------|-------------|
| Замена ролика (7 мин) | фиксировано 7 мин |
| Замена ролика (15 мин) | фиксировано 15 мин |
| Брак ленты / работа в дробилку | ввод времени вручную (мин) |
| Поломка | ввод времени вручную (мин) |
| Настройка / работа в дробилку | ввод времени вручную (мин) |

---

## 2. Таймлайн смены вместо лога простоев

Визуальная шкала времени смены с сегментами:

```
08:00 ├────────────┬───────┬───────────────────┬─────►20:00
      Работа       Ролик   Работа             Поломка
      63 мин       7 мин   185 мин            15 мин
```

- Сегменты чередуются: **Работа** (интервал между простоями) → **Простой** (цвет зависит от типа).
- Лог простыев остаётся, но сворачивается в аккордеон «⏱ Простои ▾» под таймлайном.

---

## 3. Баланс смены (основной блок)

### Что видит мастер

Компактный блок внизу панели машины:

```
┌─ Баланс смены ────────────────────────┐
│ ✅ Факт выпуска: [____] шт            │  ← поле ввода
│ ⏱ Прошло времени: 3ч 12мин           │
│ 📊 Должно быть: 130 шт               │  ← (прошло - простои) × такты × кавитации
│ 🔍 Разница: -30 шт                   │  ← факт - расчёт
│ ⚠️ ≈ Неучтённый простой: 15 мин      │  ← разница пересчитана в минуты
│ ████████░░░ 77%                      │  ← прогресс-бар
└───────────────────────────────────────┘
```

### Логика расчётов

1. **Прошло времени** — текущее время минус начало смены (08:00 или 20:00).
2. **Должно быть** — `(прошло_времени - записанные_простои) × такты_мин × кавитации`.
3. **Разница** — `факт_выпуска - должно_быть`.
4. **≈ Неучтённый простой** — `abs(разница) / (такты_мин × кавитации)` → минуты.

### Визуальная индикация

| Состояние | Цвет | Текст |
|-----------|------|-------|
| Разница ≈ 0 (±5%) | 🟢 Зелёный | ✔ Данные сходятся |
| Факт < Расчёт | 🔴 Красный | ⚠ Забыл указать простой? (~N мин) |
| Факт > Расчёт | 🟡 Жёлтый | Впереди графика! |

### Поле ввода «Факт выпуска»

- Числовое поле с автоформатированием (пробелы-разделители: `1 000`).
- Кэшируется в `localStorage` (`umt_balance_cache` по ключу `machine_id`).
- Восстанавливается при перезагрузке страницы.
- Обновление расчёта баланса в реальном времени при вводе.

---

## 4. Технические детали реализации

### Файлы для изменения

| Файл | Изменения |
|------|-----------|
| `templates/index.html` | Добавить блок «Баланс смены» в каждую панель машины; изменить пресеты `<select>` простоев; добавить контейнер таймлайна |
| `static/js/app.js` | Новая функция `calculateShiftBalance(machineId)`; новая функция `renderTimeline(machineId)`; localStorage кэш `umt_balance_cache`; обработчик ввода факта выпуска |
| `static/css/style.css` | Стили для блока баланса, прогресс-бара, таймлайна, цветовых состояний |
| `app.py` | Изменить пресеты типов простоев в модели/валидации (если нужно) |

### localStorage кэш

Новый ключ: `umt_balance_cache` → объект `{ "1": 100, "3": 250 }` (machine_id → pieces_produced).

### Офлайн-режим

- Значение «факт выпуска» сохраняется локально.
- Расчёт баланса полностью клиентский (не требует сервера).
- При синхронизации с сервером — пересчёт на свежих данных простоев.
